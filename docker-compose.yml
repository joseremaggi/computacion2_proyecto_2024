services:
  servidor_preguntas:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8888:8888"  # Mapea 8888 (host) → 8888 (contenedor)
    volumes:
      - ./logs:/app/logs